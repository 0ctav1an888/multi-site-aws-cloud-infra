<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Welsh Blanket Factory</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 2rem; background: #f4f4f4; }
    .card { background: #fff; padding: 2rem; border-radius: 8px; box-shadow: 0 0 10px rgba(0,0,0,0.05); }
    h1 { color: #333; }
  </style>
</head>
<body>
  <div class="card">
    <h1>Welsh Blanket Factory</h1>
    <p>Site: {{ server_site | default('unknown') | capitalize }}</p>
    <p>Instance role: {{ server_role | default('web') }}</p>
    <p>Deployed via Auto Scaling Group on {{ ansible_date_time.date }}.</p>
  </div>
</body>
</html>
